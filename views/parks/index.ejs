<!DOCTYPE html>
<html lang="en" dir="ltr">
    <head>
        <meta charset="utf-8">
        <title></title>
    </head>
    <body>
        <h1>ALL PARKS</h1>
        <ul>
            <% for(var park of parks){ %>
                <li><a href="/parks/<%= park._id %>"> <%= park.title %> - <%= park.location %> </a></li>
            <% } %>
        </ul>
    </body>
</html>
